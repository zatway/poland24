import React from 'react';
import {TextField} from "@mui/material";

const MainPage = () => {
    return (
        <div>
            <TextField></TextField>
        </div>
    );
};

export default MainPage;